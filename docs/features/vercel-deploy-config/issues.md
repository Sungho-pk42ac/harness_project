# 수직 슬라이스 이슈 — vercel-deploy-config

설정·문서 중심이라 단일 이슈로 진행한다.

## VERCEL-1 — vercel.json + 배포 가이드(DEPLOY.md)

`vercel.json`(framework=vite·build·output·SPA rewrite) + `docs/DEPLOY.md`(Supabase 준비·Vercel env·확인).
앱 로직 변경 없음. 실제 배포 실행은 사용자(계정/토큰) 몫.

## 등록된 GitHub 이슈

- VERCEL-1 → #65
