# NAA-8 (#79) — 결과 반영 + 로딩/에러 UX + 마무리

> 상당 부분이 선행 슬라이스에서 이미 충족됨. 본 슬라이스는 미충족분 보강 + 정직한 정리.

## AC 충족 현황

- **결과 양방향 반영**: ✅ 도구가 NotesContext 경유(ADR-0003) → 생성/수정/삭제가 **좌측 목록·에디터에
  즉시 반영**. 채팅에는 어시스턴트의 확인 응답(예: "회의록 노트를 만들었어요")이 표시됨. (NAA-4)
  - 메커니즘 노트: 별도 "칩" 위젯 대신 LLM 확인 메시지 + 목록 동기화로 동일 효과. 장식용 칩은 후속 폴리시.
- **로딩**: ✅ 타이핑 인디케이터(`data-testid=typing-indicator`) + 중복 전송 차단 (NAA-1)
- **에러**: ✅ onSend 실패 시 친화 메시지로 폴백, 패널 미손상 (NAA-1). 프록시 401/503은 친화 메시지로 전달 (NAA-4)
- **좁은 화면 오버레이**: ✅ `w-full max-w-md` → md 미만에서 전체폭, 이상에서 우측 도크 (NAA-1)
- **접근성(본 슬라이스 추가)**: 메시지 목록에 `aria-live="polite"` → 새 메시지를 스크린리더에 안내.

## DoD

- 전체 Vitest 그린(회귀 0), tsc 0, 디자인 시스템 clean.

Closes #79
